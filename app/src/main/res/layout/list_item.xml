<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        style="@style/CardViewStyle">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/average_padding"
            android:orientation="vertical">


            <TextView
                android:id="@+id/article_title_text_view"
                tools:text="@string/placeholder_title"
                style="@style/TitleTextViewStyle" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/publications_date_text_view"
                    tools:text="@string/placeholder_publication_time"
                    style="@style/PublicationDateTextViewStyle" />

                <TextView
                    android:id="@+id/section_name_text_view"
                    tools:text="Tech"
                    style="@style/SectionNameTextViewStyle" />

            </LinearLayout>

        </LinearLayout>

    </android.support.v7.widget.CardView>
</LinearLayout>